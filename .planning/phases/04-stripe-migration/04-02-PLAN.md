---
phase: 04-stripe-migration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
user_setup:
  - service: stripe-management
    why: "Create new webhook endpoint for management account"
    env_vars:
      - name: STRIPE_MANAGEMENT_WEBHOOK_SECRET
        source: "Stripe Dashboard (management account) > Developers > Webhooks > new endpoint > Signing secret > Reveal"
    dashboard_config:
      - task: "Create new webhook endpoint pointing to new Supabase URL"
        location: "Stripe Dashboard (management account) > Developers > Webhooks > Add endpoint"
  - service: stripe-ad-spend
    why: "Create new webhook endpoint for ad spend account"
    env_vars:
      - name: STRIPE_AD_SPEND_WEBHOOK_SECRET
        source: "Stripe Dashboard (ad spend account) > Developers > Webhooks > new endpoint > Signing secret > Reveal"
    dashboard_config:
      - task: "Create new webhook endpoint pointing to new Supabase URL"
        location: "Stripe Dashboard (ad spend account) > Developers > Webhooks > Add endpoint"

must_haves:
  truths:
    - "Both Stripe accounts have new webhook endpoints pointing to the new Supabase project edge function URLs"
    - "New webhook signing secrets are configured in the new project vault for both accounts"
    - "Stripe test events delivered to new endpoints return 200 responses"
    - "All 7 required event types are registered on each new billing webhook endpoint"
  artifacts:
    - path: "Supabase secrets (remote)"
      provides: "STRIPE_MANAGEMENT_WEBHOOK_SECRET and STRIPE_AD_SPEND_WEBHOOK_SECRET with new whsec_ values"
  key_links:
    - from: "Stripe Management account webhook endpoint"
      to: "https://qcunascacayiiuufjtaq.supabase.co/functions/v1/stripe-billing-webhook"
      via: "Stripe Dashboard webhook configuration"
    - from: "Stripe Ad Spend account webhook endpoint"
      to: "https://qcunascacayiiuufjtaq.supabase.co/functions/v1/stripe-billing-webhook"
      via: "Stripe Dashboard webhook configuration"
    - from: "stripe-billing-webhook function"
      to: "STRIPE_MANAGEMENT_WEBHOOK_SECRET / STRIPE_AD_SPEND_WEBHOOK_SECRET"
      via: "Deno.env.get() for signature verification"
      pattern: "verifySignature.*managementSecret|adSpendSecret"
---

<objective>
Create new Stripe webhook endpoints for both accounts pointing to the new Supabase project, configure signing secrets, and verify with Stripe test events.

Purpose: This is the core migration action -- re-pointing Stripe's event delivery to the new backend. Both management and ad spend accounts get new endpoints simultaneously. Old endpoints remain active as fallback (disabled in Phase 6).
Output: Both Stripe accounts delivering events to new Supabase project, signature verification passing, test events returning 200.
</objective>

<execution_context>
@/Users/forren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stripe-migration/04-RESEARCH.md
@.planning/phases/04-stripe-migration/04-CONTEXT.md
@.planning/phases/04-stripe-migration/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Stripe webhook endpoints and provide signing secrets</name>
  <action>
The user must create new webhook endpoints in both Stripe Dashboard accounts. Claude cannot access Stripe Dashboard.

**IMPORTANT:** Do NOT modify or delete the old webhook endpoints. Create NEW endpoints alongside the old ones. Old endpoints stay active as fallback until Phase 6 cutover.
  </action>
  <instructions>
**For the Management Stripe account:**

1. Log in to the Stripe Dashboard for the management account
2. Navigate to: Developers > Webhooks
3. Click "Add endpoint"
4. Set Endpoint URL to: `https://qcunascacayiiuufjtaq.supabase.co/functions/v1/stripe-billing-webhook`
5. Click "Select events" and add ALL of these event types:
   - `invoice.paid`
   - `invoice.payment_failed`
   - `invoice.payment_action_required`
   - `invoice.upcoming`
   - `customer.subscription.updated`
   - `customer.subscription.deleted`
   - `payment_intent.succeeded`
6. Click "Add endpoint" to save
7. On the new endpoint page, find "Signing secret" and click "Reveal"
8. Copy the `whsec_...` value -- this is the MANAGEMENT signing secret

**Repeat for the Ad Spend Stripe account:**

1. Log in to the Stripe Dashboard for the ad spend account
2. Navigate to: Developers > Webhooks
3. Click "Add endpoint"
4. Set Endpoint URL to: `https://qcunascacayiiuufjtaq.supabase.co/functions/v1/stripe-billing-webhook`
5. Click "Select events" and add the SAME 7 event types listed above
6. Click "Add endpoint" to save
7. Copy the `whsec_...` signing secret for the AD SPEND account

**Also check both accounts for existing dispute-webhook and stripe-webhook endpoints:**
- Note which accounts have existing endpoints for `dispute-webhook` and `stripe-webhook`
- For any that exist, create corresponding new endpoints:
  - dispute-webhook URL: `https://qcunascacayiiuufjtaq.supabase.co/functions/v1/dispute-webhook`
    Events: `charge.dispute.created`, `charge.dispute.updated`, `charge.dispute.closed`, `charge.dispute.funds_reinstated`, `charge.dispute.funds_withdrawn`
  - stripe-webhook URL: `https://qcunascacayiiuufjtaq.supabase.co/functions/v1/stripe-webhook`
    Events: `checkout.session.completed`, `payment_intent.succeeded`

**Provide to Claude:**
- The management account webhook signing secret (whsec_...)
- The ad spend account webhook signing secret (whsec_...)
- Which accounts had dispute-webhook endpoints (and their new signing secrets if applicable)
- Which accounts had stripe-webhook endpoints (and their new signing secrets if applicable)
- **Explicit confirmation that all 7 event types were registered on EACH billing webhook endpoint** (invoice.paid, invoice.payment_failed, invoice.payment_action_required, invoice.upcoming, customer.subscription.updated, customer.subscription.deleted, payment_intent.succeeded)
  </instructions>
  <resume-signal>Paste the signing secrets and confirm which endpoints were created. You MUST explicitly confirm: "All 7 event types registered on Management billing endpoint: YES/NO" and "All 7 event types registered on Ad Spend billing endpoint: YES/NO". Format: "Management: whsec_XXX, Ad Spend: whsec_YYY, Management 7 events: YES, Ad Spend 7 events: YES" plus any dispute/stripe-webhook details.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure webhook signing secrets and run test events</name>
  <files>None (remote secret configuration only)</files>
  <action>
**Step 1: Set the new signing secrets on the new Supabase project.**

Using the whsec_ values provided by the user:

```bash
cd /Users/forren/workspace/copy-alphahub/alphahub-v2
supabase secrets set STRIPE_MANAGEMENT_WEBHOOK_SECRET=whsec_MANAGEMENT_VALUE --project-ref qcunascacayiiuufjtaq
supabase secrets set STRIPE_AD_SPEND_WEBHOOK_SECRET=whsec_AD_SPEND_VALUE --project-ref qcunascacayiiuufjtaq
```

Replace `whsec_MANAGEMENT_VALUE` and `whsec_AD_SPEND_VALUE` with the actual values from the user.

If the user also created dispute-webhook or stripe-webhook endpoints with their own signing secrets, note that those functions do NOT verify signatures (per research), so no additional secrets are needed for them.

**Step 2: Verify secrets are set.**

```bash
supabase secrets list --project-ref qcunascacayiiuufjtaq | grep STRIPE.*WEBHOOK_SECRET
```

Should show both STRIPE_MANAGEMENT_WEBHOOK_SECRET and STRIPE_AD_SPEND_WEBHOOK_SECRET with the new values.

**Step 3: Run Stage 1 verification -- Stripe test events.**

Ask the user to send test webhooks from the Stripe Dashboard (or use Stripe CLI if available):

For each new endpoint in the Stripe Dashboard:
1. Go to the new endpoint's page
2. Click "Send test webhook"
3. Select `invoice.paid` and send
4. Check the endpoint's "Recent deliveries" -- should show 200 response

After user confirms test events sent, verify in Supabase edge function logs:
```bash
# Check edge function logs for the stripe-billing-webhook
# Look for recent invocations showing successful signature verification
supabase functions logs stripe-billing-webhook --project-ref qcunascacayiiuufjtaq
```

The logs should show the test event being received. Note: test events use fake data, so they may fail at the business logic level (e.g., no matching billing record) -- but they should NOT fail at signature verification. Look for:
- Success: The function processes the event (even if it returns "No matching billing record" or similar business logic response)
- Failure: "Webhook signature verification failed" or 401 status -- this means the signing secret is wrong

**Step 4: Verify across both accounts.**

Confirm test events from BOTH Stripe accounts (management AND ad spend) reach the new endpoint with 200 responses. The stripe-billing-webhook uses dual-secret verification -- it tries management secret first, then ad spend secret. Both must work.
  </action>
  <verify>
1. `supabase secrets list --project-ref qcunascacayiiuufjtaq | grep STRIPE.*WEBHOOK_SECRET` shows both secrets
2. Stripe Dashboard shows 200 responses for test events sent to new endpoints (user confirms)
3. Edge function logs show events being received without signature verification failures
  </verify>
  <done>Both webhook signing secrets are configured. Test events from both Stripe accounts are delivered to and processed by the new endpoints with 200 responses. No signature verification failures.</done>
</task>

</tasks>

<verification>
1. Both Stripe accounts (management + ad spend) have new webhook endpoints registered at `https://qcunascacayiiuufjtaq.supabase.co/functions/v1/stripe-billing-webhook`
2. Old webhook endpoints remain active (not modified or deleted)
3. STRIPE_MANAGEMENT_WEBHOOK_SECRET and STRIPE_AD_SPEND_WEBHOOK_SECRET are set with new whsec_ values on the new project
4. Stripe test events from both accounts show 200 responses in the Dashboard delivery logs
5. Edge function logs confirm event processing without signature verification errors
6. All 7 event types confirmed registered on each billing webhook endpoint
</verification>

<success_criteria>
- New webhook endpoints exist in both Stripe Dashboard accounts pointing to new Supabase project URL
- All 7 event types registered on each billing webhook endpoint (confirmed by user)
- Both signing secrets configured as Supabase secrets on new project
- Test event delivery confirmed with 200 responses from both accounts
- Old endpoints untouched (fallback for Phase 6)
</success_criteria>

<output>
After completion, create `.planning/phases/04-stripe-migration/04-02-SUMMARY.md`
</output>
