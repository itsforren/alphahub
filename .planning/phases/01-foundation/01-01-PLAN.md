---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AlphaHub/AlphaHub.xcodeproj/project.pbxproj
  - AlphaHub/App/AlphaHubApp.swift
  - AlphaHub/App/AppState.swift
  - AlphaHub/Core/Auth/.gitkeep
  - AlphaHub/Core/Design/.gitkeep
  - AlphaHub/Core/Navigation/.gitkeep
  - AlphaHub/Features/Login/.gitkeep
  - AlphaHub/Features/Shell/.gitkeep
  - AlphaHub/Features/Placeholder/PlaceholderView.swift
  - AlphaHub/Shared/Components/.gitkeep
  - AlphaHub/Shared/Extensions/.gitkeep
  - AlphaHub/Shared/Modifiers/.gitkeep
  - AlphaHub/Resources/Assets.xcassets
  - AlphaHub/Resources/Fonts/.gitkeep
  - AlphaHub/Info.plist
autonomous: false
user_setup:
  - service: apple-developer
    why: "Apple Developer Program enrollment required for TestFlight and App Store"
    env_vars: []
    dashboard_config:
      - task: "Verify Apple Developer Program enrollment ($99/year)"
        location: "https://developer.apple.com/account — check Membership tab"
      - task: "Note Team ID for Xcode signing"
        location: "https://developer.apple.com/account — Membership > Team ID"

must_haves:
  truths:
    - "Xcode project opens without errors and builds for iOS 17+ simulator"
    - "All three SPM dependencies resolve and compile (supabase-swift, KeychainAccess, Kingfisher)"
    - "Folder structure matches the architecture from research (App/, Core/, Features/, Shared/, Resources/)"
    - "App launches on simulator and shows a placeholder screen"
  artifacts:
    - path: "AlphaHub/AlphaHub.xcodeproj/project.pbxproj"
      provides: "Xcode project configuration"
    - path: "AlphaHub/App/AlphaHubApp.swift"
      provides: "@main app entry point"
      contains: "@main"
    - path: "AlphaHub/Info.plist"
      provides: "App configuration with Face ID usage description"
      contains: "NSFaceIDUsageDescription"
    - path: "AlphaHub/Features/Placeholder/PlaceholderView.swift"
      provides: "Temporary placeholder view for app launch verification"
  key_links:
    - from: "AlphaHub.xcodeproj"
      to: "SPM dependencies"
      via: "Package dependencies in project settings"
      pattern: "supabase-swift|KeychainAccess|Kingfisher"
    - from: "AlphaHubApp.swift"
      to: "PlaceholderView.swift"
      via: "body scene renders placeholder"
---

<objective>
Create the Xcode project skeleton with SPM dependencies, folder structure, and build verification.

Purpose: Establish the project foundation that every subsequent plan builds on. Without a working Xcode project with resolved dependencies, no other work can proceed.
Output: A building iOS project targeting iOS 17+ with supabase-swift, KeychainAccess, and Kingfisher as SPM deps, the full folder hierarchy from research, and a placeholder app that launches on simulator.
</objective>

<execution_context>
@/Users/forren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with SPM dependencies and folder structure</name>
  <files>
    AlphaHub/AlphaHub.xcodeproj/project.pbxproj
    AlphaHub/App/AlphaHubApp.swift
    AlphaHub/App/AppState.swift
    AlphaHub/Core/Auth/.gitkeep
    AlphaHub/Core/Design/.gitkeep
    AlphaHub/Core/Navigation/.gitkeep
    AlphaHub/Features/Login/.gitkeep
    AlphaHub/Features/Shell/.gitkeep
    AlphaHub/Features/Placeholder/PlaceholderView.swift
    AlphaHub/Shared/Components/.gitkeep
    AlphaHub/Shared/Extensions/.gitkeep
    AlphaHub/Shared/Modifiers/.gitkeep
    AlphaHub/Resources/Assets.xcassets
    AlphaHub/Resources/Fonts/.gitkeep
    AlphaHub/Info.plist
  </files>
  <action>
    Create a new Xcode project in the repo root at `AlphaHub/`:

    1. **Create the Xcode project** using `xcodebuild` or by generating the project structure manually:
       - Product name: `AlphaHub`
       - Bundle ID: `com.alphaagent.ios`
       - Deployment target: iOS 17.0
       - Swift language version: 6.0
       - Interface: SwiftUI
       - Lifecycle: SwiftUI App

    2. **Add SPM dependencies** to the project:
       - `https://github.com/supabase/supabase-swift.git` — from: "2.0.0"
       - `https://github.com/kishikawakatsumi/KeychainAccess.git` — from: "4.2.2"
       - `https://github.com/onevcat/Kingfisher.git` — from: "8.0.0"

    3. **Create the folder structure** (matching research architecture):
       ```
       AlphaHub/
       ├── App/
       │   ├── AlphaHubApp.swift      # @main entry point
       │   └── AppState.swift          # Root app state (@Observable) — stub
       ├── Core/
       │   ├── Auth/                   # .gitkeep (populated in plan 01-02)
       │   ├── Design/                 # .gitkeep (populated in plan 01-03)
       │   └── Navigation/            # .gitkeep (populated in plan 01-03)
       ├── Features/
       │   ├── Login/                  # .gitkeep (populated in plan 01-02)
       │   ├── Shell/                  # .gitkeep (populated in plan 01-03)
       │   └── Placeholder/
       │       └── PlaceholderView.swift  # Temporary "Hello Alpha Hub" view
       ├── Shared/
       │   ├── Components/             # .gitkeep
       │   ├── Extensions/             # .gitkeep
       │   └── Modifiers/              # .gitkeep
       └── Resources/
           ├── Assets.xcassets         # Default asset catalog
           └── Fonts/                  # .gitkeep (fonts added in plan 01-03)
       ```

    4. **Configure Info.plist** with required keys:
       - `NSFaceIDUsageDescription`: "Alpha Hub uses Face ID to protect your financial data"
       - `UILaunchStoryboardName`: (empty or remove — using SwiftUI lifecycle)
       - App Transport Security: Allow arbitrary loads = NO (HTTPS only)

    5. **AlphaHubApp.swift** — minimal @main entry:
       ```swift
       import SwiftUI

       @main
       struct AlphaHubApp: App {
           var body: some Scene {
               WindowGroup {
                   PlaceholderView()
               }
           }
       }
       ```

    6. **PlaceholderView.swift** — simple verification view:
       ```swift
       import SwiftUI

       struct PlaceholderView: View {
           var body: some View {
               ZStack {
                   Color.black.ignoresSafeArea()
                   VStack(spacing: 16) {
                       Image(systemName: "bolt.fill")
                           .font(.system(size: 48))
                           .foregroundColor(.white)
                       Text("Alpha Hub")
                           .font(.title)
                           .fontWeight(.bold)
                           .foregroundColor(.white)
                       Text("Coming soon")
                           .font(.subheadline)
                           .foregroundColor(.gray)
                   }
               }
               .preferredColorScheme(.dark)
           }
       }
       ```

    7. **AppState.swift** — stub for root state:
       ```swift
       import Observation

       @Observable
       final class AppState {
           var isReady = false
       }
       ```

    **IMPORTANT:** Use `swift package resolve` or `xcodebuild -resolvePackageDependencies` to ensure all three SPM packages download and resolve correctly. This is the most failure-prone step.

    **Do NOT use xcodegen, tuist, or any third-party project generation tool.** Create the Xcode project natively — either through `xcodebuild` commands or by manually constructing the .xcodeproj/project.pbxproj. If creating manually is impractical via CLI, use the simplest available method and document how it was created.
  </action>
  <verify>
    1. `ls AlphaHub/AlphaHub.xcodeproj/project.pbxproj` — file exists
    2. `xcodebuild -project AlphaHub/AlphaHub.xcodeproj -scheme AlphaHub -destination 'platform=iOS Simulator,name=iPhone 16' -resolvePackageDependencies` — resolves without error
    3. `xcodebuild -project AlphaHub/AlphaHub.xcodeproj -scheme AlphaHub -destination 'platform=iOS Simulator,name=iPhone 16' build` — builds successfully
    4. Verify folder structure: `find AlphaHub -name "*.swift" -o -name ".gitkeep" | sort`
    5. `grep NSFaceIDUsageDescription AlphaHub/Info.plist` — key exists
  </verify>
  <done>
    Xcode project builds for iOS 17+ simulator with all three SPM dependencies resolved, folder structure matches architecture spec, Info.plist has NSFaceIDUsageDescription, and PlaceholderView renders on launch.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Xcode project skeleton with SPM dependencies (supabase-swift, KeychainAccess, Kingfisher), full folder hierarchy, and a placeholder app that builds and launches.
  </what-built>
  <how-to-verify>
    1. Open `AlphaHub/AlphaHub.xcodeproj` in Xcode
    2. Verify Package Dependencies section shows 3 packages (Supabase, KeychainAccess, Kingfisher)
    3. Select iPhone 16 simulator, press Cmd+R to build and run
    4. Confirm app launches with black screen showing "Alpha Hub" text and bolt icon
    5. Check Project Navigator — folder structure should match: App/, Core/, Features/, Shared/, Resources/
    6. Verify signing: if you have Apple Developer account, set Team in Signing & Capabilities
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `xcodebuild build` exits with BUILD SUCCEEDED
- All SPM packages resolve without errors
- App launches on iOS simulator
- Folder structure matches research spec
- Info.plist contains NSFaceIDUsageDescription
</verification>

<success_criteria>
A building Xcode project at AlphaHub/ that launches on iOS 17+ simulator with a placeholder screen, three SPM dependencies resolved, and the full folder hierarchy ready for subsequent plans to populate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
